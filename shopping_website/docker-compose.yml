version: '3.8'

services:
  web:
    build: .
    ports:
      - "5000:5000"
    volumes:
      # Mount database to persist data between restarts
      - ./database:/app/database
    environment:
      - FLASK_APP=app.py
      - FLASK_ENV=development
